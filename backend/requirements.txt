# FaceCheckin Backend Requirements
# Python: use 3.10 or 3.11 on Windows for insightface wheels/build compatibility.
# Install with: pip install -r requirements.txt

# Web Framework
aiohttp>=3.8.0
aiofiles>=23.0.0

# Face Recognition
# NOTE:
# - insightface>=0.7.3 thuong khong co wheel san tren mot so mirror,
#   dan den pip phai build C++ va can Visual C++ Build Tools.
# - pin 0.2.1 de giu kha nang cai dat 1-click tren Windows khong can toolchain C++.
insightface==0.2.1
onnxruntime>=1.16.0
opencv-python>=4.5.0

# Data Processing
numpy>=1.20.0
matplotlib>=3.5.0
qrcode[pil]>=7.4.2

# Database
# sqlite3 is built-in with Python

# Optional: For production deployment
# gunicorn>=20.1.0
# python-dotenv>=0.19.0
